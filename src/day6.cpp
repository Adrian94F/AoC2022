#include <bits/stdc++.h>
#include <iostream>

using namespace std;


int day6() {
    ifstream input("./day6.txt");
    if (!input.is_open()) {
        cout << "Unable to open file";
        return -1;
    }

    string line;
    while (getline(input,line)) {

    }
    input.close();

    return 0;
}
